package banco;

import java.util.Scanner;

public class Main {
	public static void main(String[] args) {
		ContaCorrente contaCorrente;
		contaCorrente = new ContaCorrente();
		Scanner input = new Scanner(System.in);

		System.out.print("informe o número e a agência da conta: ");
		contaCorrente.iniciarContaCorrente(input.nextInt(), input.nextInt());
		
		System.out.print("Valor do deposito: ");
		contaCorrente.depositar(input.nextDouble());
		
		System.out.println("Conta: " + contaCorrente.exibeConta());
		System.out.println("Agencia: " + contaCorrente.exibeAgencia());
		
		contaCorrente.sacar(300);
		
		System.out.println("Saldo: " + contaCorrente.consultarSaldo());
	}
}